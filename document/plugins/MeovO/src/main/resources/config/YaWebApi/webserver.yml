target-server-host: "127.0.0.1"
target-server-port: 8080

allowedIpAddresses:
  - "127.0.0.1"

allowedOrigins:
  - "http://127.0.0.1:8080"
  - "https://your-frontend.com"

secret-key: "This is a secret key that only the server and the client know."
